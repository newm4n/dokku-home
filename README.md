# Dokku Home (solarch.work)

Beforu you `make push` :

```bash
git remote add dokku dokku@solarch.work:solarch.work
```

and make sure your SSH password-less works
against `dokku@solarch.work`

Then you can run :

```bash
$ make push
```